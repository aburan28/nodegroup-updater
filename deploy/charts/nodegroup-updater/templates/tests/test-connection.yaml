apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodegroup-updater.fullname" . }}-test-connection"
  labels:
    {{- include "nodegroup-updater.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodegroup-updater.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
